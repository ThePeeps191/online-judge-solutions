#include <iostream>

using namespace std;

void process(int x) {
    while(x > )) {
        cout << x % 3;
    }
}

int main() {
    while (true) {
        int N;
        cin >> N;
        if(N < 0) break;
        process(N);
    }
}