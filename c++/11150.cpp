#include <iostream>

using namespace std;

int main() {
    int num;
    cin >> num;
    int sum = num;
    int num1 = 0;
    while (num >= 3) {
        int p = num / 3;
        sum += p;
        int t = num % 3;
        num 
    }
}